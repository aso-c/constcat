# constcat
"Literally" constexpr concatenation string literals and/or (potentially) C-like string or other similar buffer objects in compile time.  
Recursive implementation: split input arrays to a set of items by recusive call of the variadic template procedures.

Obtain code:

`$ git clone https://github.com/aso-c/constcat <out_dir>`

