# constcat
"Statically" constexpr concatenation string literals and/or (potentially) C-like string or other similar buffer objects in compile time.
Obtain code:

`$ git clone https://github.com/aso-c/constcat <out_dir>`